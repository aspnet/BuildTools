phases:
- template: phases/default-build.yml
  parameters:
    agentOs: macOS
- template: phases/default-build.yml
  parameters:
    agentOs: Windows
- template: phases/default-build.yml
  parameters:
    agentOs: Linux
