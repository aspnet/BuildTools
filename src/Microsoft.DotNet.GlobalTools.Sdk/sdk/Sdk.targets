<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
  </PropertyGroup>

  <Target Name="WarnAboutThisSdk" BeforeTargets="Build">
    <Warning Text="Microsoft.DotNet.GlobalTools.Sdk is obsolete. The features it provides are now part of the default SDK. You can remove the reference to this and set PackAsToolShimRuntimeIdentifiers=win-x86;win-x64" />
  </Target>
</Project>
